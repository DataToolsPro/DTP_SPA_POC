# =============================================================================
# DTP_SPA_POC â€” SPA Environment Variables (Vite)
# =============================================================================
#
# WARNING: THIS FILE IS A TEMPLATE ONLY -- NEVER PUT REAL VALUES HERE
#
# HOW TO USE:
#   Copy this file to dtp/.env.local (gitignored)
#   Get real values from 1Password vault: DTP_SPA_POC -- Dev Secrets
#
# VITE RULES:
#   - ALL variables exposed to the browser MUST be prefixed with VITE_
#   - Variables without VITE_ prefix are NOT available in React code
#   - dtp/.env.local     = local dev (gitignored)
#   - dtp/.env           = shared defaults (safe to commit if no secrets)
#   - GitHub Environment Variables set VITE_* at build time for staging/prod
#
# =============================================================================

# --- API -------------------------------------------------------------------
# URL of the Laravel backend API
# Local dev: http://localhost:8000/api
# Staging:   injected by GitHub Actions (vars.VITE_API_URL)
# Prod:      injected by GitHub Actions (vars.VITE_API_URL)
VITE_API_URL=http://localhost:8000/api

# --- App -------------------------------------------------------------------
VITE_APP_NAME="DataTools Pro"
VITE_APP_ENV=local

# --- Cloudflare (if needed client-side) -----------------------------------
# Usually NOT needed in the SPA -- Cloudflare operates at the edge.
# Only add here if a specific Cloudflare SDK requires it.
# VITE_CLOUDFLARE_ACCOUNT_ID=

# --- Feature Flags ---------------------------------------------------------
# Use these to toggle features per environment.
# Keep values as true/false strings.
VITE_FEATURE_AI_ASSISTANT=false
VITE_FEATURE_DARK_MODE=false

# --- Analytics / Monitoring -----------------------------------------------
# Only enable in staging/production builds -- set via GitHub Variables
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=