# =============================================================================
# DTP_SPA_POC -- Environment Variable Reference
# =============================================================================
#
# WARNING: THIS FILE IS A TEMPLATE ONLY -- NEVER PUT REAL VALUES HERE
#
# PURPOSE:
#   This file is the single source of truth for WHAT environment variables
#   the application needs. It is safe to commit and should always be kept
#   up to date as new variables are added to the codebase.
#
# HOW TO USE:
#   1. Copy this file to .env  (repo root, never committed)
#   2. Get the real values from the shared 1Password vault:
#      `DTP_SPA_POC -- Dev Secrets`
#   3. Ask @rmgoodm for vault access if you don't have it yet
#
# RULES:
#   OK  .env.example  --> commit freely, always keep up to date
#   NO  .env          --> NEVER commit, already in .gitignore
#   NO  Real keys, passwords, tokens --> NEVER in this file
#
# WHERE SECRETS LIVE:
#   Local dev      --> .env on your machine (sourced from 1Password vault)
#   CI/CD          --> GitHub Actions Secrets (Settings > Secrets > Actions)
#   Staging env    --> GitHub Environment: staging
#   Production env --> GitHub Environment: production
#
# =============================================================================

# -----------------------------------------------------------------------------
# Laravel -- Core
# -----------------------------------------------------------------------------
APP_NAME="DataTools Pro"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_LOCALE=en
APP_TIMEZONE=UTC

# -----------------------------------------------------------------------------
# Database -- AWS RDS (MySQL 8)
# -----------------------------------------------------------------------------
DB_CONNECTION=mysql
DB_HOST=
DB_PORT=3306
DB_DATABASE=dtp_spa_poc
DB_USERNAME=
DB_PASSWORD=

# Local dev alternative (SQLite, no server needed):
# DB_CONNECTION=sqlite
# DB_DATABASE=/absolute/path/to/database.sqlite

# -----------------------------------------------------------------------------
# Cache, Session and Queue
# -----------------------------------------------------------------------------
CACHE_STORE=database
SESSION_DRIVER=database
SESSION_LIFETIME=120
QUEUE_CONNECTION=database

# Redis (uncomment when added)
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=
# REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Mail
# -----------------------------------------------------------------------------
MAIL_MAILER=log
MAIL_FROM_ADDRESS="noreply@datatoolspro.com"
MAIL_FROM_NAME=""

# SMTP (fill in for staging/production)
# MAIL_HOST=
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_ENCRYPTION=tls

# -----------------------------------------------------------------------------
# AWS -- S3 for file/media storage (NOT for hosting the app)
# Used by Laravel Filesystem driver: Storage::disk('s3')
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_URL=

# -----------------------------------------------------------------------------
# Cloudflare
# GitHub Secret:   CLOUDFLARE_API_TOKEN
# GitHub Variable: CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_ZONE_ID
# -----------------------------------------------------------------------------
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_PAGES_PROJECT=

# -----------------------------------------------------------------------------
# Cloudways -- SSH deployment details
# Actual SSH keys live in GitHub Secrets, not here.
# Listed for documentation so devs know what to configure.
# -----------------------------------------------------------------------------
# CLOUDWAYS_HOST=
# CLOUDWAYS_USER=
# CLOUDWAYS_APP_PATH=

# -----------------------------------------------------------------------------
# AI / LLM Providers
# GitHub Secrets: OPENAI_API_KEY, ANTHROPIC_API_KEY
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_DEFAULT_MODEL=gpt-4o

ANTHROPIC_API_KEY=
ANTHROPIC_DEFAULT_MODEL=claude-3-5-sonnet-20241022

# Optional LLM observability
# HELICONE_API_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# React SPA -- Vite (all SPA vars must be prefixed with VITE_)
# These go in spa/.env.local -- listed here as reference
# -----------------------------------------------------------------------------
# VITE_APP_ENV=local
# VITE_API_URL=http://localhost:8000/api
# VITE_APP_NAME="DataTools Pro"

# -----------------------------------------------------------------------------
# Staging overrides (configured in GitHub Environment: staging)
# -----------------------------------------------------------------------------
# APP_ENV=staging
# APP_DEBUG=false
# APP_URL=https://staging.datatoolspro.com
# VITE_API_URL=https://staging.datatoolspro.com/api

# -----------------------------------------------------------------------------
# Production overrides (configured in GitHub Environment: production)
# -----------------------------------------------------------------------------
# APP_ENV=production
# APP_DEBUG=false
# APP_URL=https://app.datatoolspro.com
# VITE_API_URL=https://app.datatoolspro.com/api