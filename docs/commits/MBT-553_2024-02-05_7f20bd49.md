# jira 553 Revised Auth Process and potentially passing along OTP token as state parameter

| Field       | Value |
|-------------|-------|
| Commit      | `7f20bd4973f3479de4babdcf9cb7649e653f4bba` |
| Date        | 2024-02-05 15:18:27 +0500 |
| Author      | waqarcs11 <adsknock@gmail.com> |
| Jira Ticket | MBT-553 |
| Parents     | `2d37acfa8a7a26a64cf59b869049993ed29b4c24` |

## Message



## Files Changed

```
app/Http/Controllers/SalesforceController.php    | 10 +++++-----
 app/Providers/AppServiceProvider.php             |  1 +
 app/Providers/CustomSocialiteServiceProvider.php |  9 ++++++---
 config/app.php                                   |  1 -
 4 files changed, 12 insertions(+), 9 deletions(-)
```

## AI Summary

1. **What changed**:
   - Updated the `redirectToSalesforce` method in `SalesforceController.php` to pass the OTP token as a `state` parameter instead of `otp`.
   - Modified the `callback` method in `SalesforceController.php` to handle the incoming `state` parameter.
   - Adjusted the `CustomSocialiteServiceProvider` to bind the `Laravel\Socialite\Contracts\Factory` to `App\Providers\CustomSocialiteProvider`.
   - Removed the `CustomSocialiteServiceProvider` from the `app.php` configuration file.

2. **Why**: The changes likely aim to revise the authentication process by using the OTP token as a state parameter for better security and handling.

3. **Category**: Feature
