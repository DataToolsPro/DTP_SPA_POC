# jira 689 to 681

| Field       | Value |
|-------------|-------|
| Commit      | `b23b7a32a5e6426a13919471f30a3df2e8c7e661` |
| Date        | 2024-05-01 18:29:31 +0500 |
| Author      | waqarcs11 <adsknock@gmail.com> |
| Jira Ticket | MBT-689 |
| Parents     | `860abc484824330bfdc32820877098ddd32edac2` |

## Message



## Files Changed

```
app/Http/Controllers/MetricsController.php         | 42 ++++++++++++----------
 app/Http/Controllers/ObjectFieldsController.php    | 40 +++++++++++++--------
 app/Models/MetricAlias.php                         |  9 +++++
 ...dd_field_deleted_option_to_change_type_enum.php |  5 ++-
 ..._01_125336_update_metric_api_response_table.php | 32 +++++++++++++++++
 public/assets/css/custom.css                       | 35 ++++++++++++++++++
 resources/views/pages/metrics.blade.php            |  1 +
 7 files changed, 131 insertions(+), 33 deletions(-)
```

## AI Summary

1. **What changed**:
   - Refactored the `show` method in `MetricsController` to use a new method `getAliasesByMetricId` from the `MetricAlias` model for fetching aliases.
   - Updated the `showMetricDiagram` method to change the `key` and `parent` values from integers to strings for consistency.
   - Modified the `index` method in `ObjectFieldsController` to use a dynamic SQL query instead of a static one for fetching mapped fields.
   - Added a new migration to update the `metric_api_responses` table to allow nullable fields.
   - Cleaned up the `change_type` column in the `object_fields_audit_logs` table migration.

2. **Why**: The changes aim to improve code maintainability and consistency, as well as to enhance database schema flexibility.

3. **Category**: Refactor
