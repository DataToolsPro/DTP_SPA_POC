# jira 553 Revised Auth Process and potentially passing along OTP token as state parameter

| Field       | Value |
|-------------|-------|
| Commit      | `72c9d9cbb965811e699f88eee075d675294325cc` |
| Date        | 2024-02-05 12:44:28 +0500 |
| Author      | waqarcs11 <adsknock@gmail.com> |
| Jira Ticket | MBT-553 |
| Parents     | `44e12e87172291afe03670668efaf3d6baf16691` |

## Message



## Files Changed

```
app/Http/Controllers/SalesforceController.php | 5 ++---
 1 file changed, 2 insertions(+), 3 deletions(-)
```

## AI Summary

1. **What changed**:
   - Updated the `redirectToSalesforce` method in `SalesforceController.php` to use `$request->session()->put()` instead of `session()->put()` for storing the `state` parameter.
   - Removed a debug statement (`dd($request)`) from the `callback` method in `SalesforceController.php`.

2. **Why**: The changes likely aim to improve the handling of session data and clean up the code by removing unnecessary debug output.

3. **Category**: Refactor
