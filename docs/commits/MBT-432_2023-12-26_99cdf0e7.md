# jira 432 Vulnerability 5: Stored XSS in Definition of Dictionary

| Field       | Value |
|-------------|-------|
| Commit      | `99cdf0e77d3b6aef7ac6b04dd842bd14645412d2` |
| Date        | 2023-12-26 16:44:24 +0500 |
| Author      | waqarcs11 <adsknock@gmail.com> |
| Jira Ticket | MBT-432 |
| Parents     | `c83b999330570b2eed98b250d47b4900010ebe62` |

## Message



## Files Changed

```
app/Http/Controllers/ObjectFieldsController.php |  10 +-
 app/Http/Controllers/ScorecardsController.php   |   9 +-
 composer.json                                   |   2 +
 composer.lock                                   | 141 +++++++++++++++++++++++-
 4 files changed, 153 insertions(+), 9 deletions(-)
```

## AI Summary

1. **What changed**:
   - Added HTML purification to the `updateSingleObject` method in `ObjectFieldsController` for the `logic`, `notes`, and `definition` fields using the `Mews\Purifier` package.
   - Updated the `edit` method in `ScorecardsController` to use `strip_tags` for the `metrics` and `notes` fields.
   - Added `mews/purifier` and `ezyang/htmlpurifier` as dependencies in `composer.json`.

2. **Why**: This commit addresses a stored XSS vulnerability by sanitizing user input in specific fields.

3. **Category**: Bug Fix
